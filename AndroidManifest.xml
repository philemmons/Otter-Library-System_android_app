<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vader.proj2part3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        android:name=".OLS"

        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".CreateAccountDisplay" />
        <activity android:name=".PlaceHoldDisplay" />
        <activity android:name=".CancelHoldDisplay" />
        <activity android:name=".ManageSystemDisplay" />
        <activity android:name=".AddBookToLibrary" />


    </application>

</manifest>